module.exports = {
  name: "help",
  code: `
  $title[Help Message]
  $description[💰 **Economy :**
  \`work\`, \`beg\`, \`bal\`, \`profile\`, \`deposit\`, \`withdraw\`, \`daily\`, \`open-daily\`, \`heist\`, \`givemoney\`, \`shop\`, \`buy-<object>\`, \`open-<chest>\`, \`rob\`, \`steal\`, \`search\`, \`srap-<object>\`, \`filp-<object>\`, \`fish\`, \`lb-bank\`, \`lb-wallet\`

  🏆 **Leveling :**
  \`setrank\`, \`resetrank\`, \`setrankmsg\`, \`rank\`

  🌴 **Moderation :**
  \`ban\`, \`unban\`, \`kick\`, \`clear\`, \`warn\`, \`warnings\`, \`unwarn\`, \`temprole\`, \`giverole\`, \`removerole\`, \`setmute\`, \`mute\`, \`unmute\`, \`tempmute\`

  🔩 **Custom Commands :**
  \`add-cmd\`, \`del-cmd\`, \`cmd-list\`

  🎶 **Music :**
  \`play\`, \`volume\`, \`queue\`, \`skip\`, \`clearqueue\`, \`stop\`, \`nowplaying\`, \`loop\`, \`resume\`, \`pause\`

  📜 **Tickets :**
  \`ticket-setup\`, \`ticket\`, \`close\`

  👋 **Welcoming :**
  \`setjoinchannel\`, \`setleavechannel\`, \`setleavemessage\`, \`setjoinmessage\`

  🔐 **AutoMod :**
  \`antilink\`

  🎭 **Role Reactions :**
  \`channel\`, \`message\`, \`add\`, \`del\`, \`logs on/off\`, \`dm on/off\`

  💌 **Invite Tracker :**
  \`setinvitejoinchannel\`, \`setinviteleavechannel\`, \`invites-lb\`, \`inviter\`, \`invites\`
  
  :books: **ModLog :**
  \`setup-modlogs\`

  :tada: **Giveaways :** *beta*
  \`giveaway\`

  ℹ️ **Informations :**
  \`botinfo\`, \`channelinfo\`, \`serverinfo\`, \`devinfo\`, \`roleinfo\`, \`userinfo\`

  🦩 **Utility :**
  \`snipe\`, \`editsnipe\`, \`quote\`, \`addemoji\`, \`invite\`

  🐍 **Fun :**
  \`guess-number\`, \`gtnstats\`, \`guessthenumber\`, \`disable-guessthenumber\`, \`howgay\`
  
  ⛔ **Admins :**
  \`setprefix\`]
  $addTimestamp
  $color[RED]
  $footer[lost]
  `
}
